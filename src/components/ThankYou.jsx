import React from 'react';

const ThankYou = ({ title, image, description }) => {
  console.log('title', title);
  console.log('image', image);
  console.log('description', description);

  return <div>Thank You!</div>;
};

export default ThankYou;
